# Optum Bank front door (concept)

Static single-page marketing site prototype for optumbank.com - the unified consumer/provider front door. No build step: it is a self-contained index.html.

Deployed on Vercel from this folder (set the project Root Directory to `optumbank-site`).
